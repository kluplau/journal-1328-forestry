---
label: Slider
hide_body: true
fields:
- name: images
  type: field_group_list
  fields:
  - name: image
    type: file
    config:
      maxSize: 250
    label: image
  - name: headline
    type: text
    config:
      required: false
    label: headline
  config:
    min: 
    max: 
    labelField: 
  label: images
